<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bruno.debarrio">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- <uses-permission android:name="android.permission.HttpURLConnection" /> -->


    <!-- EXTERNAL_STORAGE permissions are optional for Android 6.0 onwards. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="22" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="22" />

    <!-- AIzaSyAuJY44MzeWCWREBKTHAQuq66h7bz0BJPw -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">

            <!--
<intent-filter>
<action android:name="android.intent.action.MAIN" />

<category android:name="android.intent.category.LAUNCHER" />
</intent-filter>
            -->

        </activity>
        <activity
            android:name=".MainTabbedActivity"
            android:label="@string/title_activity_main_tabbed"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistroActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCqZ9zKjUMTiWVcPAVDuEL8WqFs3NHFm-E" /> <!-- android:value="@string/google_maps_key"  AIzaSyAuJY44MzeWCWREBKTHAQuq66h7bz0BJPw -->
        <!-- android:name="com.google.android.maps.v2.API_KEY" -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps">

            <!--
          <intent-filter>
              <action android:name="android.intent.action.MAIN" />

              <category android:name="android.intent.category.LAUNCHER" />
          </intent-filter>
            -->

        </activity>
        <activity android:name=".AddContactoActivity" />
        <activity android:name=".AddEmailActivity" />
        <activity android:name=".AddTelefonoActivity" />
        <activity android:name=".AddDireccionActivity" />
        <activity android:name=".TelefonosActivity" />
        <activity android:name=".DireccionesActivity" />
        <activity android:name=".FotoElegirActivity" />
        <activity android:name=".AddFotoElegirActivity" />
        <activity android:name=".EmailsActivity" />
        <activity android:name=".ContactosActivity" />
        <activity android:name=".NoIncluidos.IdiomaActivity" />
        <activity android:name=".EventosTodosActivity" />
        <activity android:name=".EventosMiosActivity" />
        <activity android:name=".UsuariosActivity"></activity>
    </application>

</manifest>